/** Default organization UUID used for bootcamp / demo bootstrap. */
export const DEFAULT_ORG_ID = "00000000-0000-0000-0000-000000000001";

/** Cookie name for multi-org active workspace (httpOnly, server-set). */
export const ACTIVE_ORG_COOKIE = "opsflow-active-org-id";

/** Legacy template id reference (must exist in DB for flows that use it). */
export const DEFAULT_TEMPLATE_ID = "00000000-0000-0000-0000-000000000002";
